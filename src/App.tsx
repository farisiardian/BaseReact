import React from 'react';
import AppRouter from './routes/router'; // Import the renamed router

const App: React.FC = () => {
  return <AppRouter />; // Use the AppRouter component here
};

export default App;
